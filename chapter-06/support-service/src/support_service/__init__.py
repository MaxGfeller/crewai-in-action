"""Customer 360 + KB service for the Gmail support Flow.

This package is a self-contained FastAPI service. The Flow in
``../gmail_support_flow`` consumes it over HTTP. Treat the two as
separate applications - do not import across that boundary.
"""
